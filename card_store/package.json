{
  "name": "@noxiannet/card-store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --pool=threads",
    "import:riftcodex": "tsx scripts/import-riftcodex.ts",
    "refresh:derived-identities": "tsx scripts/refresh-derived-identities.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "chevrotain": "^12.0.0",
    "fastify": "^5.6.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
